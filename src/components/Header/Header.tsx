import { FC } from "react";

const Header: FC = () => {
  return (
    <header className="header">
      <span className="logo">
        Логотип
      </span>
    </header>
  );
}

export default Header;
